# Переименовать файл из ".env.example" в ".env" и всё заработает
# Logging
LOG_LEVEL=local

# HTTP/GRPC
HTTP_PORT=8080
PROMETHEUS_PORT=9000
GRPC_PORT=3000

# Database connection details
DB_HOST=postgres# менять localhost на postgres(название контейнера с бд) если использую docker
DB_USER=postgres
DB_PASSWORD=root
DB_NAME=avito_pvz
DB_PORT=5432
DB_SSL=disable

# JWT
JWT_SECRET=08a354669c7dde0b72b0c4264ffa62601de13858cbf68591a00e2949d21f9b1b

# Auto-generated DB URL
PG_URL=postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=${DB_SSL}

# Migrations
MIGRATIONS_PATH=migrations

